Jax.Controller.create "<%=controller_name%>", ApplicationController,
  <%- actions.each do |action| -%>
  <%= action %>: ->
    # Action body here
  
  <%- end -%>
  # Some special actions are fired whenever the corresponding input is
  # received from the user.
  mouse_pressed: (event) ->
