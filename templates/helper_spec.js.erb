describe("<%=class_name%>Helper", function() {
  var helper;
  
  beforeEach(function() {
    var klass = Jax.Class.create({ helpers: [ <%=class_name%>Helper ] });
    helper = new klass();
  });

  it("should sum 1+1", function() {
    expect(helper.sum(1, 1)).toEqual(2);
  });
});
