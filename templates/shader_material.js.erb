Jax.Material.<%=class_name%> = Jax.Class.create(Jax.Material.Layer, {
  numPasses: function() { return 1; },
  
  setVariables: function(context, mesh, options, vars, pass) {
    // vars.VariableName = value;
  }
});
