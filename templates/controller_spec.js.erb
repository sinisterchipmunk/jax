describe("<%= class_name %>Controller", function() {
  var controller;
  
  beforeEach(function() {
    this.context.redirectTo("<%= file_name %>");
  });
  
  it("should do something", function() {
    expect(this.context.controller).toBeInstanceOf(Jax.Controller);
  });
});
