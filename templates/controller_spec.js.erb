describe("<%=class_name%>Controller", function() {
  var controller;
  
  beforeEach(function() {
    SPEC_CONTEXT.redirectTo("<%=file_name%>/index");
    controller = SPEC_CONTEXT.current_controller;
  });
  
  it("should do something", function() {
    expect(controller).toBeKindOf(Jax.Controller);
  });
});
