/* matrix uniforms */
shared uniform mat4 mMatrix, ivMatrix, mvMatrix, pMatrix;
shared uniform mat3 vnMatrix, nMatrix;

/* material uniforms */
shared uniform vec4 materialDiffuse, materialAmbient, materialSpecular;
shared uniform float materialShininess;
      
shared uniform int PASS_TYPE;
      
/* light uniforms */
shared uniform vec3 LIGHT_DIRECTION, LIGHT_POSITION;
shared uniform vec4 LIGHT_SPECULAR, LIGHT_AMBIENT, LIGHT_DIFFUSE;
shared uniform bool LIGHT_ENABLED;
shared uniform int LIGHT_TYPE;
shared uniform float SPOTLIGHT_COS_CUTOFF, SPOTLIGHT_EXPONENT, LIGHT_ATTENUATION_CONSTANT, LIGHT_ATTENUATION_LINEAR,
              LIGHT_ATTENUATION_QUADRATIC;
      
/* shadow map uniforms */
shared uniform bool SHADOWMAP_ENABLED;
shared uniform sampler2D SHADOWMAP0, SHADOWMAP1;
shared uniform mat4 SHADOWMAP_MATRIX;
shared uniform bool SHADOWMAP_PCF_ENABLED;
shared uniform float DP_SHADOW_NEAR, DP_SHADOW_FAR;

shared varying vec2 vTexCoords;
shared varying vec3 vNormal, vLightDir, vSpotlightDirection, vTbnDirToLight;
shared varying vec3 vTangent, vBitangent;
shared varying vec4 vBaseColor, vShadowCoord;
shared varying float vDist;
      
shared varying vec3 vDP0, vDP1;
shared varying float vDPz, vDPDepth;
