describe("NoiseController", function() {
  var controller;

  beforeEach(function() {
    controller = new NoiseController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
