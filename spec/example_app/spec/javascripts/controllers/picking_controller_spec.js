describe("PickingController", function() {
  var controller;

  beforeEach(function() {
    controller = new PickingController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
