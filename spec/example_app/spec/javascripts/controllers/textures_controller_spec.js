describe("TexturesController", function() {
  var controller;

  beforeEach(function() {
    controller = new TexturesController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
