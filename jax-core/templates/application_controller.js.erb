(function() {
  if (!window.<%= Rails.application.class.name %>)
    window.<%= Rails.application.class.name %> = {};

  function <%= Rails.application.class.name %>.ApplicationController() {
    Jax.Controller.call(this);
  }

  <%= Rails.application.class.name %>.ApplicationController.prototype = new Jax.Controller();
})();
