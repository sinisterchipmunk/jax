describe "<%= Rails.application.class.name %>.<%= class_name %>", ->
  controller = null
  
  beforeEach ->
    @controller = new <%= Rails.application.class.name %>.<%= class_name %>()
    @context.redirect @controller

  it "should do something", ->
    expect(@context.controller).toBeInstanceOf(Jax.Controller)
