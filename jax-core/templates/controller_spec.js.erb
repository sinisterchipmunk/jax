describe("<%= Rails.application.class.name %>.<%= class_name %>", function() {
  var controller;
  
  beforeEach(function() {
    controller = new <%= Rails.application.class.name %>.<%= class_name %>();
    this.context.redirect(controller);
  });
  
  it("should do something", function() {
    expect(this.context.controller).toBeInstanceOf(Jax.Controller);
  });
});
