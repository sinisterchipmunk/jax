shared attribute vec4 VERTEX_POSITION;

//= require "functions/quaternions"

shared uniform vec4 mvRotation;
shared uniform vec3 mvPosition;

const float LOG2 = 1.442695;

void main(void) {
  gl_Position = pMatrix * vec4(quatMult(mvRotation, VERTEX_POSITION.xyz) + mvPosition, 1);
}
